import React from "react";

import Header from "../Header";
import SearchBar from "../SearchBar";

const HomePage = props => {
  return <SearchBar fromJobsPage={false}  />;
};

export default HomePage;
